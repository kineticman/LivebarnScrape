# Optional LiveBarn fallback credentials. You can instead save credentials in
# the admin UI after startup.
LIVEBARN_EMAIL=
LIVEBARN_PASSWORD=

# Optional HTTP Basic authentication for the admin UI and /api/* routes.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

# Docker host/public settings.
LAN_IP=192.168.1.100
SERVER_PORT=5000
